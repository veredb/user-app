User - app

In This Ruby on Rails  web app the database information is imported from a csv file (lib/tasks/Master-small.csv), via rake task.

The web app allows paging through data contained in the model, using the gem 'will_paginate'.

The app allows CRUD operations on the model.

This app includes rspec tests, supporting the TDD process.



